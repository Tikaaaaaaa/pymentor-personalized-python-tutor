# Primary hosted provider
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# Local fallback already installed on the project machine
OLLAMA_BASE_URL=http://127.0.0.1:11434
OLLAMA_MODEL=qwen3:4b

# auto = Groq when configured, otherwise Ollama
LLM_PROVIDER=auto
TUTOR_DB_PATH=data/tutor_memory.db
